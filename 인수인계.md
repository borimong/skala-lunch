# skala-lunch 이식 작업 — 인수인계 (2026-09-02 기준)

세션 끊기면 이 파일 읽고 이어서 하면 됨. 전체 계획은
`~/skala-meal-macros/계획.md` 참고(이식 설계 원본).

**2026-09-07 경로 변경 주의**: `skala-meal-macros`가 원래
`~/Downloads/Raphael_work/skala-meal-macros`에 있었는데, macOS가 `~/Downloads`를
백그라운드 프로세스(cron/launchd)에 특별 보호해서 자동화가 계속 막히는 문제 때문에
`~/skala-meal-macros`(홈 디렉토리 바로 밑)로 옮김. 이 저장소(`skala-lunch`) 위치는
안 바뀜.

## 이 저장소가 뭔지

김현수님 소유 `borimong/skala-lunch` (React + Cloudflare Workers + D1 + Gemini).
사용자가 collaborator로 초대받아서 클론함. 우리 개인 파이썬 프로젝트
(`skala-meal-macros`)의 영양정보 계산 로직을 여기로 이식하는 게 목표.

## 지금 상태

- `main`을 최신으로 pull 받음 (2026-09-01, 김현수님 쪽에서 엑셀 업로드/ingest,
  admin 승인 페이지, openapi 문서, CI/CD workflow, 테스트 코드 대거 추가된 걸
  가져옴 — `worker/ingest.ts`, `worker/excel.ts`, `worker/validate.ts`,
  `worker/verify.ts` 등 신규)
- `feature/nutrition-info` 브랜치 새로 만들어서 지금 여기 있음 (커밋 없음, main과
  동일 상태)
- **아직 코드 작성 시작 안 함**

## 병목 — 코딩 시작 전에 반드시 확인해야 할 것

`계획.md` 7번 섹션에 정리된 4가지를 **김현수님과 아직 상의 못 함**:
1. AI 공급자: Gemini 통일 vs GPT 유지
2. 식약처 API 키: 우리 개인 키 재사용 vs 그분 명의 신규 발급
3. 비용 확인
4. UI 노출 방식: 요리별 다 보여줄지 vs 접이식

→ **다음 할 일**: 이 4개를 정리해서 Slack 메시지 초안 만들고 사용자가 김현수님께
보내는 것. 아직 초안도 안 만들어짐.

## 최근 main에 들어온 변경사항이 계획에 영향 줄 수 있음 (미확인)

`worker/ingest.ts`, `worker/excel.ts` 등 엑셀 업로드/승인 흐름이 새로 생겼는데,
이게 메뉴 저장 시점(`saveWeek()`)과 관계가 있을 수 있음. **계획.md의 "3. 데이터가
언제 계산되는가" 섹션(발행 시점에 계산하기로 한 결정)이 이 새 흐름과 충돌 없는지
아직 검토 안 했음.** 코딩 시작 전에 `worker/ingest.ts`, `worker/menus.ts` 읽어보고
`saveWeek()` 호출 지점이 여전히 유효한지 확인 필요.

## 진행 순서 (계획.md 8번 섹션 그대로)

1. `dish_nutrition` D1 마이그레이션
2. `foodHeuristics.ts` 포팅
3. `nutritionDb.ts` 포팅
4. `nutritionAi.ts` 포팅 (AI 공급자 정해진 후)
5. `nutrition.ts` 오케스트레이션
6. `saveWeek()`에 연결 (← 위 "미확인" 항목 때문에 재검토 필요할 수 있음)
7. `DishList.tsx`에 표시
8. `wrangler dev`로 로컬 테스트 → PR

## 참고 — 파이썬 원본 위치

`~/skala-meal-macros/` — `nutrition_db.py`, `estimate_gpt.py`,
`food_heuristics.py`, `estimate_macros.py`가 포팅 대상 원본. 이 폴더는
저장소에 커밋하지 않기로 함(참고만, `sample/` 폴더 같은 거 만들지 않기로
사용자가 결정함).

## 지시사항 (반드시 지킬 것)

- `../6주차_클라우드/day2/homework_revised/`는 이 작업과 무관, 절대 건드리지 말 것
  (다른 Claude 세션 "복습공간"이 전담)
