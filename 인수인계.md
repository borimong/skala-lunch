# skala-lunch 영양정보 이식 — 인수인계 (2026-09-10 기준)

세션 끊기면 이 파일 읽고 이어서 하면 됨. 예전 버전 기록은 `과거_인수인계.md`
참고(지금은 그 내용 대부분 최신화됨, 역사적 기록용).

## 지금 상태 — 요약

**⭐ 현재 버전이 지금까지 중 제일 잘 도는 버전.** 실제 오늘자 메뉴로
발행→계산→상세페이지→Slack 전송까지 end-to-end 검증 완료(2026-09-10).

- 브랜치: `feature/nutrition-info` (origin에 push 완료, main은 안 건드림)
- 최신 커밋: `eae702b` "feat: Gemini tool-calling 에이전트로 전환"
- 김현수님 저장소(`borimong/skala-lunch`)에 대한 collaborator 권한으로 작업 중

## 아키텍처 — 지금 이렇게 동작함

**"고정 규칙"이 아니라 "LLM 에이전트 + tool calling"** 구조로 완전히 정착됨.

```
발행(POST /api/menus) 직후
  → collectMeals(): WeeklyMenu를 끼니(중식/석식) 단위 배열로 변환
  → ensureNutrition(): 끼니마다 캐시 확인 → 없으면 runMealAgent() 호출
      → Gemini에게 끼니 요리 목록 전체를 한 번에 보여줌
      → Gemini가 lookup_food_db 도구를 필요한 만큼 호출(우리 코드가 실제
        apis.data.go.kr 조회를 대신 실행해서 결과를 돌려줌)
      → 최종 JSON 답변(요리별 serving_g/탄단지/source/reliability + exclude)
  → D1(dish_nutrition, nutrition_trace)에 저장
```

핵심 파일:
- `worker/nutrition.ts` — 오케스트레이션 + Gemini tool-calling 루프(`runMealAgent`,
  `callGemini`, `SYSTEM_PROMPT`). **이 프로젝트의 핵심 로직 전부 여기 있음.**
- `worker/nutritionDb.ts` — 식약처 DB(data.go.kr) 실제 조회(`lookupBest`). AI가
  호출을 "요청"하면 이 함수가 진짜 HTTP GET을 실행함(AI는 직접 조회 안 함).
- `worker/foodHeuristics.ts` — `isOutlier`만 남음(카테고리별 kcal 상한선 검증).
  예전에 있던 `guessServingG`/`consumptionRatio`(고정 비율 규칙)는 에이전트
  구조로 넘어가면서 완전히 제거됨 — 필요하면 git log에서 복원 가능하지만
  지금 방향과는 안 맞음.
- `src/pages/NutritionPage.tsx` — 상세페이지(`/nutrition/:date`), 김현수님
  "상세화면 디자인가이드" 그대로 따름(표 + 출처배지 + 이상치배지 + 제외항목 취소선).
- `migrations/0002~0004` — dish_nutrition(복합키 date+meal_type+food_name,
  source/reliability/outlier/excluded_reason 컬럼) + nutrition_trace 테이블.

## AI 판단 흐름 (사용자가 확인한 요약, 정확함)

1. 검색 전에 요리명을 단어 단위로 쪼갤지 스스로 판단 (예: "고추잡채*꽃빵" →
   "고추잡채"+"꽃빵" 두 번 검색 — 실제 테스트에서 관찰됨, 우리가 시키지 않음)
2. `lookup_food_db` 도구를 호출 요청 (실제 API 조회는 우리 코드가 대신 실행)
3. 필요하면 1~2를 여러 번 반복(최대 8턴)
4. 그래도 못 찾으면 AI가 자기 지식으로 직접 추정(source: `llm_estimate`)

메인/반찬 판단, 국밥+쌀밥 중복 감지도 전부 **숫자 규칙이 아니라 자연어
"경향성" 지침**(`SYSTEM_PROMPT`)만 주고 AI가 자율 판단함.

## AI 공급자 — OpenAI에서 Gemini로 교체됨 (중요)

- OpenAI 계정 크레딧이 소진돼서(insufficient_quota) 2026-09-10에 Gemini로 전환.
- **새 시크릿 `GEMINI_NUTRITION_API_KEY`**: 사용자 본인 명의로 Google AI Studio
  (https://aistudio.google.com/apikey)에서 무료 발급받은 키. 사진→메뉴 추출용
  `GEMINI_API_KEY`(김현수님 명의)와는 **완전히 별개** — 그분 할당량 안 건드림.
- 로컬 테스트: `.dev.vars`에 `GEMINI_NUTRITION_API_KEY=` 자리 있음, 각자 채워넣기.
  **`.dev.vars`는 `.claude/settings.json`의 deny 규칙으로 Claude가 못 읽게 막아둠**
  — 다음 세션도 이 파일 내용을 절대 Read하려 하지 말 것(설정이 막아주지만,
  실수로 다른 경로로 값을 출력하지 않도록 주의).
- 실서비스 배포 시 `wrangler secret put GEMINI_NUTRITION_API_KEY` 필요.

## 로컬 테스트 방법 (재현 절차)

```bash
cd /Users/raphael807/Downloads/Raphael_work/skala-lunch
rm -rf .wrangler/state/v3/d1   # 로컬 D1 초기화하고 싶을 때만
npx wrangler d1 migrations apply skala-lunch --local
npx vite build
npx wrangler dev --port 8787   # 백그라운드 권장

# 실제 오늘 메뉴로 발행 테스트 (김현수님 프로덕션 API에서 가져와서 그대로 씀)
curl -s https://skala-lunch.ewkimhyunsu11.workers.dev/api/menus/current -o /tmp/menu.json
# 필요하면 python으로 특정 날짜 하루만 추려서 /tmp/publish_today.json 만들고
curl -X POST http://localhost:8787/api/menus \
  -H "Authorization: Bearer <ADMIN_TOKEN(.dev.vars 값)>" \
  -H "Content-Type: application/json" --data @/tmp/publish_today.json

curl "http://localhost:8787/api/nutrition?date=YYYY-MM-DD&mealType=lunch"
```

**주의**: 끼니당 Gemini 호출 1~수 턴 + DB조회 여러 번이라 발행 API가 10~90초
정도 걸릴 수 있음(레이트리밋 없으면 빠름). `curl --max-time 180` 정도로
여유 두고 기다릴 것 — 중간에 끊으면 실패로 오인하기 쉬움.

외부(폰 등)에서 로컬 서버를 확인하고 싶으면 `cloudflared tunnel --url
http://localhost:8787`로 임시 공개 URL을 뜰 수 있음(컴퓨터/터널 꺼지면 죽는
임시 링크, 실배포 아님).

## 알려진 문제 / 겪었던 오류 (재발 방지용 기록)

- **프로덕션엔 이 기능이 아직 배포된 적이 없음(2026-09-14 확인)**: 매일 아침
  오는 진짜 Slack 알림은 김현수님 프로덕션 워커(`main` 브랜치, cron
  `0 23 * * *` = KST 08시)가 보내는 거라 영양정보가 낄 자리가 없음. 로컬
  (`localhost:8787`)에서만 테스트해온 것이지, `feature/nutrition-info`가
  실서비스에 배포/머지된 적은 없음 — "왜 Slack에 영양정보가 안 나오냐"는
  질문이 계속 나올 수 있는데 매번 이걸로 답하면 됨.
- **`POST /api/menus` 발행 시 payload에 여러 날짜(`days`)를 한꺼번에 넣으면
  안 됨**: `ensureNutrition`이 `collectMeals(parsed.data.days)`로 payload에
  담긴 모든 날의 모든 끼니를 다 처리한다. 실수로 한 주(5일×2끼=10끼니) 전체를
  넣었더니 Gemini 무료 쿼터를 불필요하게 많이 쓰고, curl이 시간 초과로
  연결을 놓침(2026-09-14 재발). 로컬 테스트 발행 시 반드시 `days` 배열을
  테스트하려는 그 날짜 하나로만 잘라서 보낼 것.
- **Gemini 503("model is currently experiencing high demand")**: 429(쿼터
  초과)와는 다른 에러 — 일시적 서버 과부하라 재시도하면 대개 해결됨. 429와
  구분해서 로그 읽을 것(429=쿼터/한도, 503=일시적 혼잡).
- **로컬 Ollama 경로 실측 결과(`experiment/ollama-local-llm` 브랜치 참고)**:
  `qwen3:8b`(추론형)는 응답 90초+로 사실상 사용 불가, `qwen2.5:3b`는
  74초/끼니로 빠르지만 JSON 스키마(`serving_g` 등) 누락 발생 — 소형 모델의
  instruction-following 한계로 실사용은 보류. Gemini 쿼터(일일 20회) 소진
  시 스모크테스트 용도로만 남겨둠. **주의**: 이 실험 코드는 안정 버전
  (`feature/nutrition-info`)엔 안 섞여 있고 별도 브랜치로 분리해둠.

## 코딩기록 (`worker/nutrition.ts`에서 뺀 설계 히스토리)

코드 안 주석이 너무 길어서 정리하면서 여기로 옮긴 내용. 코드는 "무엇을
하는지"만 간결하게 남기고, "왜 이렇게 됐는지"는 여기서 관리한다.

- **설계 전환(2026-09-10)**: "메인/반찬 비율을 코드가 고정 숫자(90/40/20%)로
  강제하는" 방식을 버리고, LLM에게 tool calling으로 식약처 DB 조회 권한을
  주는 에이전트 구조로 바꿈(Spring AI의 `@Tool`과 동일한 개념 — LLM이 도구
  목록을 보고 필요할 때 호출, 결과를 반영해서 최종 답변). 끼니 전체를 한 번에
  LLM에게 보여주고, "첫 1~2개(isMain)는 메인메뉴로 그 양만큼 먹는다 / 나머지는
  반찬으로 훨씬 적게 먹는다 / 메인이 국밥·비빔밥류인데 밥이 따로 있으면 중복
  가능성을 판단한다"는 **경향성**만 지침으로 주고, 정확한 비율은 LLM이
  요리 특성에 맞게 판단하게 함.
- **검증은 코드가 한다는 원칙**: LLM이 만든 값이라도 최종 kcal는 항상 코드가
  탄×4+단×4+지×9로 재계산(LLM 산술 오류 방지), 이상치 판단(`foodHeuristics.
  isOutlier`)도 코드가 독립적으로 한 번 더 검증해서 배지를 붙인다 — "숫자는
  LLM이 만들어도 검증은 코드가 한다"는 원칙 유지.
- **OpenAI → Gemini 교체(2026-09-10)**: OpenAI 계정 크레딧이
  소진돼서(`insufficient_quota`) LLM을 Gemini로 교체함. 이 저장소에 이미
  사진→메뉴 추출(`worker/gemini.ts`)용 Gemini가 있었지만 그건 김현수님 명의
  키(`GEMINI_API_KEY`)라서, 이 기능이 그분 무료 할당량을 갉아먹지 않도록
  별도 시크릿(`GEMINI_NUTRITION_API_KEY`, 사용자 본인 명의 무료 키)을 새로
  만듦. Gemini의 함수 호출(`tools.functionDeclarations`) API 형식은 OpenAI의
  `tools`와 달라서(`type`이 대문자 `"OBJECT"`/`"STRING"` 등, role이
  `user`/`model`만 존재, system/tool 역할이 따로 없음) 호출부를 다시 짬.
- **Gemini `contents` 배열 규칙**: role은 `"user"`(입력/도구 결과)와
  `"model"`(응답)만 존재 — OpenAI처럼 별도 `system`/`tool` role이 없고,
  시스템 지침은 `systemInstruction` 필드로, 도구 결과는
  `role:"user"`의 `functionResponse` part로 표현한다.
- **`runMealAgent`가 최대 8턴인 이유**: 요리 개수가 많으면 도구를 여러 번
  불러야 할 수 있어서 여유를 둔 값. 실측으로는 보통 2~3턴에 끝남(위 "알려진
  문제" 섹션의 호출 횟수 실측 참고).
- **캐시 재사용 원칙**: 끼니의 모든 요리가 이미 D1 캐시에 있으면
  (`mealFullyCached`) 에이전트를 다시 부르지 않음 — 쿼터 절약.
- **`ensureNutrition` 호출 위치**: `saveWeek()` 직후, 발행 시점에 딱 한 번만
  호출된다(배치/서빙 분리 원칙 유지) — `worker/index.ts`의 `POST /api/menus`.

## 아직 안 정해진 것 (계획.md 참고, `~/skala-meal-macros/계획.md`)

김현수님과 아직 상의 안 한 것들 — 언제든 다시 확인 필요:
1. UI 노출 방식(요리별 다 보여줄지 vs 접이식) — 지금은 표 형태로 다 보여줌
2. 비용/사용량 — Gemini 무료 티어 한도 안에서 운영 가능한지 실사용해봐야 앎
3. PR을 언제/어떻게 열지 — 아직 PR 안 열었음, feature 브랜치에 push만 함

## 완료됨 (2026-09-21)

- **PR #1 오픈**: `feature/nutrition-info` → `main`, 충돌 없음(MERGEABLE 확인).
  https://github.com/borimong/skala-lunch/pull/1
- **`worker/slack.ts` 실제 배포 코드에 반영**: 요리명 `*` 볼드마크다운 충돌
  수정(가운뎃점 치환) + `/nutrition/:date` 링크를 실제 Slack 알림에 추가.
  테스트 6개 추가, 전체 37개 통과.

## 다음에 할 일 후보

- 현수님 리뷰 대기 — PR에 남긴 3가지 질문(UI 확정, secret 등록 주체, Slack
  링크 추가 여부) 답 받기
- 프로덕션 secret 등록(`wrangler secret put GEMINI_NUTRITION_API_KEY`,
  `DATA_GO_KR_API_KEY`) — 머지 승인 후 진행
- (선택) Lv3 스타일 구성요소 분해를 에이전트가 잘 하는 걸 확인했으니, 이걸
  더 신뢰하고 맡겨도 될지 vs 추가 검증 로직이 필요할지 판단
