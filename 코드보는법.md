# 코드보는법 — 영양정보 기능 (`feature/nutrition-info`)

이 문서는 "어느 파일에 뭐가 있고, 어떤 순서로 읽으면 이해가 빠른지"를 정리한
가이드다. 핵심(백엔드 로직) → 그걸 뒷받침하는 것 → 화면(프론트) → 부수적인 것
순서로 배치했다. 각 파일에 이미 "왜 이렇게 짰는지" 주석이 많이 달려 있으니,
이 문서에서는 파일 간 흐름과 "무엇을 보러 가야 하는지"에 집중한다.

전체 아키텍처 요약은 `인수인계.md`에 있음 — 이 문서를 다 읽기 전에 그것부터
훑는 걸 추천.

---

## 1순위 — 핵심 로직 (여기가 진짜 이 기능의 전부)

### `worker/nutrition.ts` (397줄) ⭐ 제일 먼저, 제일 오래 볼 파일
이 기능의 두뇌. 아래 순서로 읽어라:

1. **파일 맨 위 주석(1~24줄)** — 이 기능이 왜 지금 이 구조가 됐는지 히스토리
   (고정비율 규칙 → LLM 에이전트, OpenAI → Gemini). 배경 이해에 필수.
2. **`SYSTEM_PROMPT`(160~182줄)** — Gemini에게 주는 지침 전문. "AI가 뭘 보고
   판단하는지"가 여기 다 있다. 코드 로직보다 이 프롬프트 문구를 이해하는 게
   더 중요할 수도 있음.
3. **`runMealAgent()`(187~234줄)** — 끼니 하나를 계산하는 메인 루프. 여기가
   말 그대로 "에이전트가 도구를 몇 번이고 부르는" 그 로직. 흐름:
   `callGemini` 호출 → 함수 호출 요청 있으면 `lookupBest()`(nutritionDb.ts)
   실행해서 결과를 돌려줌 → 없으면 최종 JSON으로 파싱해서 반환.
4. **`ensureNutrition()`(333~381줄)** — 바깥에서 호출하는 진입점. 캐시 확인 →
   없는 끼니만 `runMealAgent` 호출 → D1에 저장. `worker/index.ts`가 여기를 부름.
5. `callGemini()`(104~138줄) — 실제 HTTP 호출 + 429/쿼터 재시도 로직.
6. `buildTraceMarkdown()`(236~256줄) — 감사/디버깅용 추적로그를 만드는 곳.
   "AI가 뭘 조회했고 어떻게 답했는지" 원본이 여기서 만들어짐.

**나중에 봐도 되는 부분**: `runMealAgentOllama`류의 Ollama 관련 코드는
`experiment/ollama-local-llm` 브랜치에만 있고 이 브랜치(feature/nutrition-info)
에는 없음 — 혼동하지 말 것.

### `worker/nutritionDb.ts` (104줄)
식약처 공공DB 실제 조회. `lookupBest(query, serviceKey)`가 유일하게 밖에서
쓰는 함수. 식약처 API 응답을 파싱해서 100g당 칼로리/탄단지 + 검색어 유사도를
반환한다. `similarity()`는 문자열 유사도 계산(한글 bigram 기반) — 몰라도
전체 흐름 이해엔 지장 없음.

### `worker/foodHeuristics.ts` (44줄)
`isOutlier(dishName, kcalPer100g)` 하나만 있음. AI가 낸 값이 "이 요리 카테고리
치고 너무 높다" 싶으면 배지를 붙이기 위한 최후 검증 장치. 짧아서 5분이면 다 읽힘.

---

## 2순위 — 이 로직을 호출하는 곳 (API 계약)

### `worker/index.ts`의 발행/영양정보 관련 부분만
전체 299줄 중 아래만 보면 됨 (`grep -n "nutrition\|Nutrition" worker/index.ts`):
- `POST /api/menus` 핸들러 안, `saveWeek()` 직후 `ensureNutrition()` 호출부
  — **주의**: `collectMeals(parsed.data.days)`가 요청 payload에 담긴 **모든
  날짜**를 다 처리한다. 로컬 테스트 시 실수로 한 주 전체를 넣으면 Gemini
  쿼터를 순식간에 다 쓴다(실제로 겪음, `인수인계.md`의 "알려진 문제" 참고).
- `GET /api/nutrition?date=&mealType=` — 프론트가 상세페이지 그릴 때 쓰는
  공개 엔드포인트.
- `GET /api/nutrition/trace?date=&mealType=` — 관리자 전용, 추적로그(markdown)
  원본 반환.

나머지(메뉴 CRUD, 인그레스, 관리자 인증 등)는 영양정보 기능과 무관하니
지금은 안 봐도 됨.

### `migrations/0002~0004_*.sql`
D1 스키마. 순서대로: `dish_nutrition` 테이블 생성(복합키 `date+meal_type+
food_name`) → `source`/`reliability`/`outlier` 컬럼 추가 + `nutrition_trace`
테이블 생성 → `excluded_reason` 컬럼 추가. 지금 스키마가 궁금하면 0002부터
0004까지 순서대로 읽으면 진화 과정이 보임.

---

## 3순위 — 화면 (프론트엔드)

### `src/pages/NutritionPage.tsx` (185줄)
`/nutrition/:date` 라우트. 김현수님 "상세화면 디자인가이드"를 그대로 구현한
표 화면. `RELIABILITY_BADGE`(출처 배지 색)와 `excludedReason` 있으면
취소선+배지 처리하는 부분만 보면 나머지는 평범한 표 렌더링.

### `src/lib/api.ts`의 `DishNutrition` 타입 + `fetchNutrition()`
`worker/index.ts`의 `/api/nutrition` 응답 타입과 1:1 대응. 여기 타입이
`worker/nutrition.ts`의 `DishNutrition` interface와 같은 모양인지 맞춰보면
프론트-백엔드 계약이 한눈에 보임.

### `src/App.tsx`
`<Route path="/nutrition/:date" .../>` 한 줄 추가된 것만 확인하면 끝.

---

## 4순위 — 몰라도 되지만 궁금하면

- `worker/foodHeuristics.test.ts` — `isOutlier` 테스트 4개, 로직 이해의
  보조자료로 좋음(입력→출력 예시가 곧 스펙).
- `worker/slack.ts` — **아직 영양정보 기능과 연결 안 돼 있음**. 현재는
  기존 방식 그대로(메뉴+총합만) 알림 보냄. "왜 Slack에 영양정보가 안
  나오냐"는 이게 답 — `인수인계.md` 참고.
- `과거_인수인계.md` — 지금은 낡은 설계(고정비율 규칙 버전) 기록이라
  현재 코드와 안 맞음. 역사적 참고용으로만.

---

## 요약: 딱 3개 파일만 먼저 보고 싶다면

1. `worker/nutrition.ts` (특히 `SYSTEM_PROMPT`, `runMealAgent`, `ensureNutrition`)
2. `worker/index.ts`의 `POST /api/menus` 핸들러 중 영양정보 호출부
3. `src/pages/NutritionPage.tsx`

이 세 개만 봐도 "요청이 어떻게 들어와서 → AI가 어떻게 계산하고 → 화면에
어떻게 나오는지" 전체 흐름이 잡힌다.
